{
  "name": "dmitriev-brandbook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node tokens/sync.js && bash scripts/build-og-image.sh",
    "prebuild": "node tokens/sync.js && bash scripts/build-og-image.sh",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test tokens/*.test.js"
  },
  "dependencies": {
    "astro": "^4.16.0"
  }
}
